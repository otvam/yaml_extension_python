include_var: !include 'test_sub.yaml' #include the content file sub.yaml
path_var: !path 'test_sub.yaml' # parse the relative path with respect to the YAML file
exec_var: !exec '[1, 2, 3]' # evaluate the Python expression